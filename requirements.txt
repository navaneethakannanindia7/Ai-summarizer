# ============================================================
# requirements.txt
# All dependencies installable with: pip install -r requirements.txt
# ============================================================

# Core ML / NLP
scikit-learn>=1.3.0       # TF-IDF, LDA, NMF, SVD, cosine_similarity
numpy>=1.24.0             # Array operations

# Web Framework
flask>=3.0.0              # REST API server
flask-cors>=4.0.0         # Cross-origin requests

# PDF parsing (install one or more)
PyMuPDF>=1.23.0           # Best PDF text extraction (import as fitz)
# pdfminer.six>=20221105  # Alternative PDF extractor
# pypdf>=3.0.0            # Another alternative

# Optional but recommended
# gunicorn>=21.0.0        # Production WSGI server
